<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Allows SuperPodcast to search online podcast services,
        retrieve RSS feeds, and stream podcast audio.
    -->
    <uses-permission
        android:name="android.permission.INTERNET" />

    <!--
        Required on Android 13 and later so SuperPodcast
        can notify users about podcast updates.
    -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidApp4">

        <!--
            Displays podcasts saved by the user
            through the subscription feature.
        -->
        <activity
            android:name=".ui.SubscriptionsActivity"
            android:exported="false" />

        <!--
            Displays podcast information, RSS episodes,
            subscriptions, and audio playback controls.
        -->
        <activity
            android:name=".ui.PodcastDetailsActivity"
            android:exported="false" />

        <!-- Main launcher screen for SuperPodcast. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>